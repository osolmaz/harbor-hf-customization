Create `/tmp/code-mode-canary.txt` containing exactly `code-mode-ok` with no
newline. Use the `exec` Code Mode tool and call `tools.exec_command` from its
JavaScript code to write the file. Then read the file through the same tool and
report completion. Do not install software or change any other file.
