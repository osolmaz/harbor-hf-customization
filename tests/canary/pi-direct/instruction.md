Create `/tmp/pi-direct-canary.txt` containing exactly `direct-ok` with no newline.
Use Pi's `write` tool. Then read the file with Pi's `read` tool and report
completion. Do not install software or change any other file.
